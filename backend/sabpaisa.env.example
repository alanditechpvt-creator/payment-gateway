# SabPaisa Payment Gateway Configuration
# Get these credentials from SabPaisa merchant dashboard

# Enable/Disable SabPaisa
SABPAISA_ENABLED=false

# Set to 'true' for production environment
SABPAISA_PRODUCTION=false

# SabPaisa Credentials
SABPAISA_CLIENT_CODE=your-client-code        # 5-character merchant code (e.g., DJ020)
SABPAISA_USERNAME=your-trans-username        # Transaction username (e.g., DJL754@sp)
SABPAISA_PASSWORD=your-trans-password        # Transaction password

# Encryption Keys (MUST be BASE64 encoded)
SABPAISA_AUTH_KEY=your-base64-auth-key       # BASE64 encoded encryption key (32 bytes when decoded)
SABPAISA_AUTH_IV=your-base64-auth-iv         # BASE64 encoded HMAC key

# Callback URL - Where SabPaisa will send payment status
# For development: Use ngrok or similar service for public URL
# For production: Use your domain
SABPAISA_CALLBACK_URL=http://localhost:4100/api/webhooks/sabpaisa

# Example with ngrok (for testing):
# SABPAISA_CALLBACK_URL=https://your-ngrok-url.ngrok.io/api/webhooks/sabpaisa

# Example production:
# SABPAISA_CALLBACK_URL=https://yourdomain.com/api/webhooks/sabpaisa

# Important Notes:
# 1. AUTH_KEY and AUTH_IV must be BASE64 encoded strings
# 2. If they don't look like base64 (random chars, possibly ending with =), 
#    ask SabPaisa for the correct format
# 3. Callback URL must be publicly accessible (not localhost for production)
# 4. Use staging URL for testing before going live
